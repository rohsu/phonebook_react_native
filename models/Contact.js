class Contact {
    constructor(id, name, number, image, latitude, longitude) {
        this.id = id,
        this.name = name,
        this.number = number,
        this.image = image
        this.latitude = latitude
        this.longitude = longitude
    }
}

export default Contact;